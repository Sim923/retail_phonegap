/**
 * @fileOverview Primary-level ui-routes configuration
 * @name sell.landing.routes.js
 * @author Matthew Aaron Raymer
 * @license UNLICENSED
 */
(function () {
  'use strict';
  var RTMobileApp = angular.module('RTMobileApp');
  RTMobileApp.config(
    [
      '$stateProvider',
      '$urlRouterProvider',
      function($stateProvider, $urlRouterProvider) {
      }
    ]
  );
})();
